#include <stdio.h>
#include <time.h>

int main(int argc, char* argv[]) {
    printf("Hello world!\n");

    /*unsigned long x = (unsigned long)time(NULL);
    printf("Before for %lu\n", x);
    int i;
    int j = 0;
    for (i = 0; i < 100000000; ++i)
    	++j;
    unsigned long y = (unsigned long)time(NULL);
    printf("After for %lu\n", y);
    printf("Difference: %lu\n", y - x);*/

    return 0;
}
